{"metadata":{"kernelspec":{"language":"python","display_name":"Python 3","name":"python3"},"language_info":{"name":"python","version":"3.12.12","mimetype":"text/x-python","codemirror_mode":{"name":"ipython","version":3},"pygments_lexer":"ipython3","nbconvert_exporter":"python","file_extension":".py"},"kaggle":{"accelerator":"none","dataSources":[],"dockerImageVersionId":31234,"isInternetEnabled":true,"language":"python","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"# This Python 3 environment comes with many helpful analytics libraries installed\n# It is defined by the kaggle/python Docker image: https://github.com/kaggle/docker-python\n# For example, here's several helpful packages to load\n\nimport numpy as np # linear algebra\nimport pandas as pd # data processing, CSV file I/O (e.g. pd.read_csv)\n\n# Input data files are available in the read-only \"../input/\" directory\n# For example, running this (by clicking run or pressing Shift+Enter) will list all files under the input directory\n\nimport os\nfor dirname, _, filenames in os.walk('/kaggle/input'):\n    for filename in filenames:\n        print(os.path.join(dirname, filename))\n\n# You can write up to 20GB to the current directory (/kaggle/working/) that gets preserved as output when you create a version using \"Save & Run All\" \n# You can also write temporary files to /kaggle/temp/, but they won't be saved outside of the current session","metadata":{"_uuid":"8f2839f25d086af736a60e9eeb907d3b93b6e0e5","_cell_guid":"b1076dfc-b9ad-4769-8c92-a6c4dae69d19","trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.621494Z","iopub.execute_input":"2026-01-09T18:00:44.622743Z","iopub.status.idle":"2026-01-09T18:00:44.628563Z","shell.execute_reply.started":"2026-01-09T18:00:44.622702Z","shell.execute_reply":"2026-01-09T18:00:44.627594Z"}},"outputs":[],"execution_count":67},{"cell_type":"code","source":"#!pip install bing-image-downloader","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.630341Z","iopub.execute_input":"2026-01-09T18:00:44.631271Z","iopub.status.idle":"2026-01-09T18:00:44.652571Z","shell.execute_reply.started":"2026-01-09T18:00:44.631236Z","shell.execute_reply":"2026-01-09T18:00:44.651401Z"}},"outputs":[],"execution_count":68},{"cell_type":"code","source":"!mkdir data","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.653841Z","iopub.execute_input":"2026-01-09T18:00:44.654682Z","iopub.status.idle":"2026-01-09T18:00:44.788720Z","shell.execute_reply.started":"2026-01-09T18:00:44.654639Z","shell.execute_reply":"2026-01-09T18:00:44.787501Z"}},"outputs":[{"name":"stdout","text":"mkdir: cannot create directory ‘data’: File exists\n","output_type":"stream"}],"execution_count":69},{"cell_type":"code","source":"#from bing_image_downloader import downloader\n#downloader.download('potholes', limit=100,output_dir='data')","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.791062Z","iopub.execute_input":"2026-01-09T18:00:44.791417Z","iopub.status.idle":"2026-01-09T18:00:44.796069Z","shell.execute_reply.started":"2026-01-09T18:00:44.791385Z","shell.execute_reply":"2026-01-09T18:00:44.795147Z"}},"outputs":[],"execution_count":70},{"cell_type":"code","source":"#ACTIVATION FUNCTION\ndef sigmoid(z):\n    return 1.0/(1.0+np.exp(-z))\ndef sigmoid_prime(z):\n    s=sigmoid(z)\n    return s*(1-s)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.797240Z","iopub.execute_input":"2026-01-09T18:00:44.797501Z","iopub.status.idle":"2026-01-09T18:00:44.815969Z","shell.execute_reply.started":"2026-01-09T18:00:44.797476Z","shell.execute_reply":"2026-01-09T18:00:44.815107Z"}},"outputs":[],"execution_count":71},{"cell_type":"code","source":"#BINARY CROSS ENTROPY\ndef binary_cross_entropy(y_hat,y):\n    return -(y*np.log(y_hat)+(1-y)*np.log(1-y))","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.817272Z","iopub.execute_input":"2026-01-09T18:00:44.817587Z","iopub.status.idle":"2026-01-09T18:00:44.834393Z","shell.execute_reply.started":"2026-01-09T18:00:44.817555Z","shell.execute_reply":"2026-01-09T18:00:44.833230Z"}},"outputs":[],"execution_count":72},{"cell_type":"code","source":"class Network:\n    def __init__(self, sizes):\n        \"\"\"\n        sizes: list like [input, hidden, output]\n        example: [2, 3, 1]\n        \"\"\"\n        self.num_layers = len(sizes)\n        self.sizes = sizes\n\n        # initialize biases (no bias for input layer)\n        self.biases = [np.random.randn(y, 1) for y in sizes[1:]]\n\n        # initialize weights\n        self.weights = [\n            np.random.randn(y, x)\n            for x, y in zip(sizes[:-1], sizes[1:])\n        ]\n    def feedforward(self,a):\n        for b,w in zip(self.biases,self.weights):\n            a=sigmoid(np.dot(w,a)+b)\n        return a\n    def SGD(self, training_data, epochs, lr):\n        for epoch in range(epochs):\n            for x, y in training_data:\n                self.update_single(x, y, lr)\n            if epoch % 500 == 0:\n                print(f\"Epoch {epoch} complete\")\n    def backprop(self, x, y):\n        nabla_b = [np.zeros(b.shape) for b in self.biases]\n        nabla_w = [np.zeros(w.shape) for w in self.weights]\n\n        # forward pass\n        activation = x\n        activations = [x]\n        zs = []\n\n        for b, w in zip(self.biases, self.weights):\n            z = np.dot(w, activation) + b\n            zs.append(z)\n            activation = sigmoid(z)\n            activations.append(activation)\n\n        # backward pass\n        delta = (activations[-1] - y) * sigmoid_prime(zs[-1])\n        nabla_b[-1] = delta\n        nabla_w[-1] = np.dot(delta, activations[-2].T)\n\n        for l in range(2, self.num_layers):\n            z = zs[-l]\n            sp = sigmoid_prime(z)\n            delta = np.dot(self.weights[-l+1].T, delta) * sp\n            nabla_b[-l] = delta\n            nabla_w[-l] = np.dot(delta, activations[-l-1].T)\n\n        return nabla_b, nabla_w\n    def update_single(self, x, y, lr):\n        nabla_b, nabla_w = self.backprop(x, y)\n\n        self.weights = [w - lr * nw for w, nw in zip(self.weights, nabla_w)]\n        self.biases = [b - lr * nb for b, nb in zip(self.biases, nabla_b)]\n\n\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.835607Z","iopub.execute_input":"2026-01-09T18:00:44.835883Z","iopub.status.idle":"2026-01-09T18:00:44.854386Z","shell.execute_reply.started":"2026-01-09T18:00:44.835858Z","shell.execute_reply":"2026-01-09T18:00:44.853221Z"}},"outputs":[],"execution_count":73},{"cell_type":"markdown","source":"# FEEDFORWARD MLP PLAIN VANILLA","metadata":{}},{"cell_type":"code","source":"# def backprop(self,x,y):\n#     \"\"\"RETURN GRADIENTS FOR WEIGHTS AND BIASES\"\"\"\n#     nabla_b=[np.zeros(b.shape) for b in self.biases]\n#     nabla_w=[np.zeros(w.shape) for w in self.weights]\n#     activation=x\n#     ativations=[x] # STORE ACTIVATIONS LAYER BY LAYER\n#     zs=[] #STORE Z VECTOR\n#     for b,w in zip(self.biases,self.weights):\n#         z=np.dot(w)\n#         zs.append(z)\n#         activation=sigmoid(z)\n#         activation.append(activation)\n#     #BACKWARD PASS\n#     delta=(activations[-1]-y)*sigmoid_prime(zs[-1])\n#     nabla_b[-1]=delta\n#     nabla_w[-1]=np.dot(delta,activations[-2].T)\n#     #BACKPROGATe to earlier layers\n#     for l in range(2,self.num_layers):\n#         z=zs[-l]\n#         sp=sigmoid_prime(z)\n#         delta=np.dot(self.weights[-l+1].T,delta)*sp\n#         nabla_b[-l]=delta\n#         nable_w[-l]=np.dot(delta,activation[-l-1].T)\n#     return nabla_b,nabla_w","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.856636Z","iopub.execute_input":"2026-01-09T18:00:44.857466Z","iopub.status.idle":"2026-01-09T18:00:44.876345Z","shell.execute_reply.started":"2026-01-09T18:00:44.857432Z","shell.execute_reply":"2026-01-09T18:00:44.875246Z"}},"outputs":[],"execution_count":74},{"cell_type":"code","source":"    def SGD(self, training_data, epochs, lr):\n        \"\"\"\n        training_data: list of (x, y)\n        epochs: number of training passes\n        lr: learning rate\n        \"\"\"\n\n        for epoch in range(epochs):\n            np.random.shuffle(training_data)\n\n            for x, y in training_data:\n                nabla_b, nabla_w = self.backprop(x, y)\n\n                # Update parameters\n                self.weights = [\n                    w - lr * nw\n                    for w, nw in zip(self.weights, nabla_w)\n                ]\n\n                self.biases = [\n                    b - lr * nb\n                    for b, nb in zip(self.biases, nabla_b)\n                ]\n\n            print(f\"Epoch {epoch + 1} complete\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.877710Z","iopub.execute_input":"2026-01-09T18:00:44.878282Z","iopub.status.idle":"2026-01-09T18:00:44.897311Z","shell.execute_reply.started":"2026-01-09T18:00:44.878251Z","shell.execute_reply":"2026-01-09T18:00:44.896443Z"}},"outputs":[],"execution_count":75},{"cell_type":"code","source":"training_data = [\n    (np.array([[0], [0]]), np.array([[0]])),\n    (np.array([[0], [1]]), np.array([[1]])),\n    (np.array([[1], [0]]), np.array([[1]])),\n    (np.array([[1], [1]]), np.array([[0]]))\n]\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.898489Z","iopub.execute_input":"2026-01-09T18:00:44.899025Z","iopub.status.idle":"2026-01-09T18:00:44.917023Z","shell.execute_reply.started":"2026-01-09T18:00:44.898990Z","shell.execute_reply":"2026-01-09T18:00:44.916178Z"}},"outputs":[],"execution_count":76},{"cell_type":"code","source":"net = Network([2, 2, 1])\nnet.SGD(training_data, epochs=5000, lr=0.5)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:44.918283Z","iopub.execute_input":"2026-01-09T18:00:44.918627Z","iopub.status.idle":"2026-01-09T18:00:45.829194Z","shell.execute_reply.started":"2026-01-09T18:00:44.918594Z","shell.execute_reply":"2026-01-09T18:00:45.828183Z"}},"outputs":[{"name":"stdout","text":"Epoch 0 complete\nEpoch 500 complete\nEpoch 1000 complete\nEpoch 1500 complete\nEpoch 2000 complete\nEpoch 2500 complete\nEpoch 3000 complete\nEpoch 3500 complete\nEpoch 4000 complete\nEpoch 4500 complete\n","output_type":"stream"}],"execution_count":77},{"cell_type":"code","source":"for x, y in training_data:\n    prediction = net.feedforward(x)\n    print(f\"Input: {x.T} → Predicted: {prediction[0][0]:.3f} | True: {y[0][0]}\")\n","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2026-01-09T18:00:45.830288Z","iopub.execute_input":"2026-01-09T18:00:45.830680Z","iopub.status.idle":"2026-01-09T18:00:45.837232Z","shell.execute_reply.started":"2026-01-09T18:00:45.830649Z","shell.execute_reply":"2026-01-09T18:00:45.836258Z"}},"outputs":[{"name":"stdout","text":"Input: [[0 0]] → Predicted: 0.025 | True: 0\nInput: [[0 1]] → Predicted: 0.977 | True: 1\nInput: [[1 0]] → Predicted: 0.972 | True: 1\nInput: [[1 1]] → Predicted: 0.022 | True: 0\n","output_type":"stream"}],"execution_count":78}]}